# Built-in `.env` support
Run this example: `npx @uncloud/run`.

Then add new `ENV VARs` to `.env` and load them in your server.

> **Typically you should not commit your `.env` files!** It's just part of the example in this case